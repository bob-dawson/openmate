name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  android:
    name: Android APK
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Create local.properties
        working-directory: android
        run: |
          echo "RELEASE_STORE_PASSWORD=${{ secrets.RELEASE_STORE_PASSWORD }}" > local.properties
          echo "RELEASE_KEY_PASSWORD=${{ secrets.RELEASE_KEY_PASSWORD }}" >> local.properties

      - name: Build release APK
        working-directory: android
        run: ./gradlew :app:assembleRelease --no-daemon

      - name: Rename APK
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          cp android/app/build/outputs/apk/release/app-release.apk OpenMate-${VERSION}.apk

      - uses: actions/upload-artifact@v5
        with:
          name: android-apk
          path: OpenMate-*.apk

  bridge-windows:
    name: Bridge (Windows)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: opencode-bridge

      - name: Build
        working-directory: opencode-bridge
        run: cargo build --release

      - uses: actions/upload-artifact@v5
        with:
          name: bridge-windows
          path: opencode-bridge/target/release/openmate.exe

  bridge-linux:
    name: Bridge (Linux x86_64)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-musl

      - name: Install musl tools
        run: sudo apt-get update && sudo apt-get install -y musl-tools

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: opencode-bridge

      - name: Build
        working-directory: opencode-bridge
        run: cargo build --release --target x86_64-unknown-linux-musl

      - name: Rename binary
        run: cp opencode-bridge/target/x86_64-unknown-linux-musl/release/openmate openmate-linux-x86_64

      - uses: actions/upload-artifact@v5
        with:
          name: bridge-linux
          path: openmate-linux-x86_64

  bridge-linux-arm64:
    name: Bridge (Linux arm64)
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v5

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-unknown-linux-musl

      - name: Install musl tools
        run: sudo apt-get update && sudo apt-get install -y musl-tools

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: opencode-bridge

      - name: Build
        working-directory: opencode-bridge
        run: cargo build --release --target aarch64-unknown-linux-musl

      - name: Rename binary
        run: cp opencode-bridge/target/aarch64-unknown-linux-musl/release/openmate openmate-linux-arm64

      - uses: actions/upload-artifact@v5
        with:
          name: bridge-linux-arm64
          path: openmate-linux-arm64

  bridge-macos:
    name: Bridge (macOS)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: opencode-bridge

      - name: Build
        working-directory: opencode-bridge
        run: cargo build --release

      - name: Rename binary
        run: cp opencode-bridge/target/release/openmate openmate-darwin-arm64

      - uses: actions/upload-artifact@v5
        with:
          name: bridge-macos-arm64
          path: openmate-darwin-arm64

  relay-gateway:
    name: Relay Gateway (Linux)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: server/relay-gateway

      - name: Build
        working-directory: server/relay-gateway
        run: cargo build --release

      - name: Rename binary
        run: cp server/relay-gateway/target/release/relay-gateway relay-gateway-linux-x86_64

      - uses: actions/upload-artifact@v5
        with:
          name: relay-gateway-linux
          path: relay-gateway-linux-x86_64

  release:
    name: GitHub Release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [android, bridge-windows, bridge-linux, bridge-linux-arm64, bridge-macos, relay-gateway]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5

      - uses: actions/download-artifact@v5
        with:
          path: artifacts

      - name: Collect release files
        run: |
          mkdir -p release-files
          cp artifacts/android-apk/*.apk release-files/
          cp artifacts/bridge-windows/openmate.exe release-files/
          cp artifacts/bridge-linux/openmate-linux-x86_64 release-files/
          cp artifacts/bridge-linux-arm64/openmate-linux-arm64 release-files/
          cp artifacts/bridge-macos-arm64/openmate-darwin-arm64 release-files/
          cp artifacts/relay-gateway-linux/relay-gateway-linux-x86_64 release-files/
          ls -la release-files/

      - uses: softprops/action-gh-release@v2
        with:
          files: release-files/*
          generate_release_notes: true
